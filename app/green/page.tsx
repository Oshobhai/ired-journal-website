import {Header,Footer} from '../components';
import {greenPapers} from '@/lib/data';
export default function Green(){return <><Header/><section className="pageHero"><div className="container"><h1>GREEN: The Research Journal — Research Papers</h1></div></section><main className="container"><div className="contentCard"><p>Individual research papers and scholarly articles appear here after editorial approval and publication.</p><div className="listPanel">{greenPapers.map((p,i)=><div className="paperRow" key={i}><div><div className="itemTitle">{p.title}</div><div className="meta">{p.author} · {p.meta} · {p.category}</div></div><div className="actions"><button className="smallBtn">View</button><button className="smallBtn">PDF</button></div></div>)}</div></div></main><Footer/></>}
